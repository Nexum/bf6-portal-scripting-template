---
paths: .claude/rules/*.md
---

All rules files must start with a block like this:

```markdown
---
paths: **/*.ts
---
```

After that we put the actual rules.
Never use any other format than markdown.
If unsure what the paths: scope is, ask me.
